# 发布规范

适用时使用语义化版本控制。

```text
MAJOR.MINOR.PATCH
```

- **MAJOR**：破坏兼容的大改动、API 不兼容变更、核心逻辑或架构重大调整。
- **MINOR**：保持兼容的新功能、增强、UI 改进、新增可选能力。
- **PATCH**：bug 修复、小文档修正、安全维护更新。

版本号是兼容性契约，不是装饰。

每次发布最好包含概述、新增内容、变更内容、修复内容、废弃内容、移除内容、破坏性变更，以及必要的迁移说明。

